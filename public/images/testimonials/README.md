Place testimonial images here.

- Save your image files with descriptive names, e.g. `rutvik.jpg`.
- Use web-friendly formats (JPEG/PNG/WebP). Keep file sizes small (under ~200KB) for performance.

Example:
- public/images/testimonials/rutvik.jpg

After adding images, the `WhyChoose` component will automatically load them because the testimonials array references `/images/testimonials/*.jpg`.

If you'd like, I can add the actual image file here — upload it or tell me the filename to use and I'll add it for you.